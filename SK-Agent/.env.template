# =============================================================================
# SK-Agent Environment Configuration
# =============================================================================
# This file contains all environment variables needed for the Semantic Kernel Agent

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration (Required)
# -----------------------------------------------------------------------------
# Your Azure OpenAI endpoint URL
AZURE_OPENAI_ENDPOINT=https://your-openai-resource.openai.azure.com/

# Your Azure OpenAI API key
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here

# The deployment name for your model in Azure OpenAI (e.g., gpt-4, gpt-35-turbo)
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4

# API version for Azure OpenAI (recommended: 2024-02-01)
AZURE_OPENAI_API_VERSION=2024-02-01

# -----------------------------------------------------------------------------
# MCP Server Configuration (Required)
# -----------------------------------------------------------------------------
# URL of your MCP server (weather service)
MCP_SERVER_URL=https://your-weather-mcp-server.azurecontainerapps.io/sse

# API key for accessing the MCP server (if authentication is required)
MCP_API_KEY=your_mcp_server_api_key_here

# -----------------------------------------------------------------------------
# Agent Configuration (Optional - defaults provided)
# -----------------------------------------------------------------------------
# Name for your weather agent
AGENT_NAME=WeatherAgent

# Instructions for the agent behavior
AGENT_INSTRUCTIONS=You are a helpful weather assistant that can provide weather forecasts and alerts for any location. Use the provided MCP tools to get real-time weather data from the National Weather Service.

# -----------------------------------------------------------------------------
# Additional Settings (Optional)
# -----------------------------------------------------------------------------
# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Maximum tokens for responses
MAX_TOKENS=2000

# Temperature setting for AI responses (0.0 to 1.0)
TEMPERATURE=0.7
